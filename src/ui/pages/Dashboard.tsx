// Placeholder — built in Core B (live dashboard widgets).
export default function Dashboard() {
  return (
    <div class="content-head">
      <div>
        <h2 class="content-title">Dashboard</h2>
        <p class="content-sub">Cockpit overview — index strip, top volume bursts and live alerts. Coming soon.</p>
      </div>
    </div>
  )
}

