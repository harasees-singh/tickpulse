// Placeholder — built in Core B (alert delivery + recent-alerts log). The
// breakout threshold sliders currently live on the Settings screen.
export default function Alerts() {
  return (
    <div class="content-head">
      <div>
        <h2 class="content-title">Alerts</h2>
        <p class="content-sub">Delivery (toast · sound · desktop) and the recent-alerts log. Coming soon — thresholds live under Settings for now.</p>
      </div>
    </div>
  )
}

